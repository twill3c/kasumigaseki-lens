# SPEC.md — kasumigaseki-lens(霞が関レンズ)

<!-- scaffold template v1.8.0 から展開(2026-08-20)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

日本の中央省庁等 15 機関(首相官邸・内閣府・デジタル庁・金融庁・総務省・法務省・外務省・
財務省・文部科学省・厚生労働省・農林水産省・経済産業省・国土交通省・環境省・防衛省)の
最新報道発表を 1 ページで一望する。各機関の公式発表を「ヘッドライン+リンク」で 5 件ずつ
並べ、定時自動更新される静的サイトとして公開する。koho-lens(民間 IT 11 社版)の姉妹
プロジェクトであり、実証済みエンジン(feed/html/sitemap 宣言 + Actions cron + Vercel Git
連携)を移植して構築する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 15 機関それぞれの公式報道発表一覧から最新 5 件(タイトル・絶対 URL・日付)を取得できる(供給が 5 件未満のサイトは取得できた件数) | must |
| F-02 | 取得結果を `data/releases.json` に保存する(スキーマは §4 G-02。companies キーは発表主体の意) | must |
| F-03 | `out/index.html` に 15 機関 × 最新 5 件をヘッドライン(リンク付き)で一覧表示する。機関名・日付・取得時刻(JST)・出典を明記 | must |
| F-04 | GitHub Actions cron(6 時間間隔)で 取得 → 再レンダリング → コミットし、Vercel が自動配信する | must |
| F-05 | ある機関の取得が失敗しても他機関の更新は続行し、失敗した機関は前回取得分を保持する(グレースフル劣化) | must |
| F-06 | 各機関の取得経路(フィード or HTML)は `src/sources.py` の宣言的定義に集約する | should |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | ランタイム依存は Python 3.11+ 標準ライブラリのみ(pytest は dev のみ) | CI で `pip install` なしに fetch/render が動く |
| N-02 | 法務規約: 保存するのは見出し・リンク・日付のみ(本文非保存)。収集は 6 時間間隔。UA にプロジェクト URL を明記。出典明記(政府標準利用規約と整合) | コードレビュー + T-201 |
| N-03 | 1 機関の障害がジョブ全体を失敗させない | T-105 |
| N-04 | レンダリングは決定論(同じ JSON → バイト同一の HTML) | T-203 |

## 4. 品質基準

- G-01: フィクスチャ(実サイトの保存 HTML/フィード)に対し、15 機関すべてのパーサが 3 件以上を抽出し、
  タイトル非空・URL 絶対・日付 ISO 形式を満たす
- G-02: `data/releases.json` スキーマ — koho-lens と同一(companies は常に 15 要素・定義順)
- G-03: 各 item.url は https で始まり、当該機関の許可ドメインリストに属する
- G-04: fetch は一部失敗でも exit 0(全機関失敗のみ exit 1)
- G-05: HTML 出力の見出しリンク総数 = Σ items(≤75)

## 5. スコープ外

- 発表本文・添付 PDF の保存・全文検索・要約
- 過去発表のアーカイブ蓄積・時系列解析(最新 5 件のスナップショットのみ)
- JS レンダリング必須サイトへのヘッドレスブラウザ投入
- 審議会資料・統計公表など報道発表以外の新着情報(取得経路が報道発表と分離できる場合)
